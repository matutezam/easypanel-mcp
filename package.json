{
  "name": "easypanel-mcp",
  "version": "0.2.0",
  "description": "MCP server for EasyPanel — manage servers, projects, services, databases via AI",
  "type": "module",
  "bin": {
    "easypanel-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "node --import tsx --test test/**/*.test.ts",
    "generate:manifest": "tsx scripts/generate-catalog-manifest.ts",
    "audit:openapi": "tsx scripts/audit-openapi-compat.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "easypanel", "server", "hosting", "ai", "claude", "model-context-protocol"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/<your-github-user>/easypanel-mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
